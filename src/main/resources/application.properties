spring.application.name=demo

# MariaDB DataSource 설정
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://192.168.1.103:3306/security_scanner
spring.datasource.username=root
spring.datasource.password=root

# JPA (Hibernate) 설정
# spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret Key (실제 운영 시에는 환경 변수 처리 필요)
jwt.secret=this-is-a-super-secret-key-for-security-scanner-backend-change-it-later
